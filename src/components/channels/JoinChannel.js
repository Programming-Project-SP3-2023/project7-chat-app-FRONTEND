/**
 * Join Channel component
 */

/**
 * Builds and renders the join channel component
 * @returns Join Channel component render
 */
const JoinChannel = () => {
    return (
      <section className="main-section">
        <h1>Join Channel....</h1>
      </section>
    );
  };
  
  //Export the join channel component
  export default JoinChannel;
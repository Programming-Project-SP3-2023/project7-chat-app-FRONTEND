/**
 * Footer component
 */

/**
 * Builds and renders the footer component
 * @returns Footer component render
 */
const Footer = () => {
  return (
    <footer id="footer">
    </footer>
  );
};

//Export the header component
export default Footer;

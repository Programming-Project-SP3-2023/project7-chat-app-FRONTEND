/**
 * Profile component
 */

/**
 * Builds and renders the profile component
 * @returns Profile component render
 */
const Profile = () => {
  return (
    <section className="main-section">
      <h1>Profile page</h1>
    </section>
  );
};

//Export the profile component
export default Profile;

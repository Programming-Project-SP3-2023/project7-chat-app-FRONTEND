/**
 * Add Group component
 */

/**
 * Builds and renders the Add Group component
 * @returns Add Group component render
 */

const AddGroup = () => {
  return (
    <article className="dashboard-add-group">
      <h1>Add Group </h1>
    </article>
  );
};

//Export the Add Group component
export default AddGroup;

/**
 * Search component
 */

/**
 * Builds and renders the search component
 * @returns Search component render
 */
const Search = () => {
  return (
    <section className="main-section">
      <h1>Search page</h1>
    </section>
  );
};

//Export the search component
export default Search;
